{erl_opts, [debug_info]}.

{deps, [erleans,
        sbroker,
        jch]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [erleans_streams]}
]}.

{profiles, [{test, [{deps, []},
                    {erl_opts, [nowarn_export_all]}]}]}.
